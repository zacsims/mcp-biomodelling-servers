--- /home/users/simsz/.cache/uv/archive-v0/TYDk3WMDFrwbnSRvQLAgi/uq_physicell/pc_model.py	2026-04-16 15:05:52.645846095 -0700
+++ /home/users/simsz/mcp-biomodelling-servers/PhysiCell/.venv/lib/python3.10/site-packages/uq_physicell/pc_model.py	2026-04-23 04:31:08.257067864 -0700
@@ -471,6 +471,9 @@
         raise ValueError(f"An unexpected error occurred in RunModel: {e}")
 
 _attr_name_re = re.compile(r"\[@([A-Za-z_:][\w:.\-]*)")
+# Matches a bare attribute predicate with no '=' (attribute-target), e.g. rate[@index]
+# but NOT a selector like transformation_rate[@name='motile tumor'].
+_attr_target_re = re.compile(r"\[@([A-Za-z_:][\w:.\-]*)\]")
 def _get_xml_element_value(xml_root: ET.Element, key: str) -> str:
     elems = xml_root.findall(key)
     if len(elems) != 1:
@@ -483,8 +486,8 @@
                 """)
     # Get last segment of the key to extract the attribute name
     last_seg = key.rsplit("/", 1)[-1]
-    # Check and extract the attribute name using regex
-    attribute = _attr_name_re.search(last_seg)
+    # Only treat as attribute-read if predicate is a bare [@attr] (no '=').
+    attribute = _attr_target_re.search(last_seg)
     elem = elems[0]
     if attribute:
         return elem.get(attribute.group(1))
@@ -502,8 +505,10 @@
                 """)
     # Get last segment of the key to extract the attribute name
     last_seg = key.rsplit("/", 1)[-1]
-    # Check and extract the attribute name using regex
-    attribute = _attr_name_re.search(last_seg)
+    # Only treat as attribute-target if predicate is a bare [@attr] (no '=').
+    # Selector-style [@name='X'] must not be a write target — it identifies the
+    # element whose text we are setting.
+    attribute = _attr_target_re.search(last_seg)
     elem = elems[0]
     if attribute:
         elem.set(attribute.group(1), str(val))
